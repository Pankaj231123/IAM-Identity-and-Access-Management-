# IAM-Identity-and-Access-Management-
Build the stateful, device-aware session architecture. A Go REST API that handles registration, secure login, and session validation. Store user data in PostgreSQL and active sessions in Redis for high-speed lookups.
